# Electerm UI 架构分析

## 概述

Electerm 是一个基于 Electron + React 的终端/SSH/SFTP 客户端，其 UI 设计模式非常适合多功能调试工具的开发。

## 核心 UI 布局

```
┌─────────────────────────────────────────────────────┐
│  标题栏 / 工具栏                                     │
├────┬────────────────────────────────────────────────┤
│    │  [标签1] [标签2] [标签3] [+]                    │  ← 标签页（支持多开）
│ 侧 ├────────────────────────────────────────────────┤
│ 边 │                                                │
│ 栏 │                                                │
│    │           主内容区域                            │
│ 图 │      (当前标签页对应的功能界面)                 │
│ 标 │                                                │
│ 栏 │                                                │
│    │                                                │
├────┴────────────────────────────────────────────────┤
│  状态栏                                              │
└─────────────────────────────────────────────────────┘
```

## 关键组件结构

### 1. 侧边栏 (Sidebar)

**位置:** `src/client/components/sidebar/`

- **宽度:** 固定 36px 图标栏
- **组成:** 垂直排列的功能图标按钮
- **交互:** 点击图标可新建标签或展开面板

**核心文件:**
- `sidebar.jsx` - 侧边栏主组件
- `bookmark.jsx` - 书签管理
- `history.jsx` - 历史记录
- `transfer-list.jsx` - 传输列表

### 2. 标签页系统 (Tabs)

**位置:** `src/client/components/tabs/`

- **支持多实例:** 可同时打开多个同类型标签
- **批次管理:** 支持分割区域，每个区域独立标签组
- **动态创建:** 点击 [+] 或侧边栏图标新建

**核心文件:**
- `tabs.jsx` - 标签页容器
- `tab.jsx` - 单个标签组件

### 3. 布局系统 (Layout)

**位置:** `src/client/components/layout/`

**支持的布局模式:**
| 模式 | 说明 |
|------|------|
| `single` | 单窗口 |
| `two-columns` | 左右两列 |
| `three-columns` | 三列 |
| `2x2` | 2x2 网格 |

**核心文件:**
- `layout.jsx` - 布局管理器
- `window-control.jsx` - 窗口控制

### 4. 主内容区域 (Main)

**位置:** `src/client/components/main/`

- 根据当前标签类型渲染对应界面
- 支持终端、SFTP、设置等多种视图

## 状态管理

**位置:** `src/client/store/`

使用类 Redux 模式管理全局状态：
- `tabs` - 标签页列表
- `currentTabId` - 当前激活标签
- `sidebarPanelOpen` - 侧边栏面板状态
- `layout` - 当前布局模式

## 适配到 NetAssist_PySide6

### 功能映射

| 侧边栏图标 | 功能模块 | 标签类型 |
|-----------|---------|---------|
| 🌐 | 网络调试 | TCP服务端/客户端、UDP服务端/客户端 |
| 📡 | 串口助手 | Serial |
| 🔌 | I2C助手 | I2C |
| ⚙️ | SPI助手 | SPI |
| 📍 | GPIO助手 | GPIO |
| ⚙️ | 设置 | Settings |

### PySide6 实现建议

#### 侧边栏组件
```python
class Sidebar(QWidget):
    """36px 宽的图标侧边栏"""
    tab_requested = Signal(str)  # 请求新建标签信号

    def __init__(self):
        # 垂直布局，放置功能图标按钮
        pass
```

#### 标签页管理器
```python
class TabManager(QTabWidget):
    """支持多实例的标签页管理"""

    def add_tab(self, tab_type: str):
        # 根据类型创建对应的功能标签
        pass
```

#### 主窗口布局
```python
class MainWindow(QMainWindow):
    def setup_ui(self):
        # 水平布局: 侧边栏 | 主内容区
        central = QWidget()
        layout = QHBoxLayout(central)

        self.sidebar = Sidebar()           # 36px 图标栏
        self.tab_manager = TabManager()    # 标签页区域

        layout.addWidget(self.sidebar)
        layout.addWidget(self.tab_manager)
```

### 目录结构建议

```
views/
├── main_window.py          # 主窗口
├── sidebar/                # 侧边栏组件
│   ├── __init__.py
│   ├── sidebar.py          # 侧边栏主组件
│   └── sidebar_button.py   # 图标按钮
├── tabs/                   # 标签页
│   ├── __init__.py
│   ├── tab_manager.py      # 标签管理器
│   ├── tcp_server_tab.py   # TCP服务端
│   ├── tcp_client_tab.py   # TCP客户端
│   ├── udp_server_tab.py   # UDP服务端
│   ├── udp_client_tab.py   # UDP客户端
│   ├── serial_tab.py       # 串口
│   ├── i2c_tab.py          # I2C
│   ├── spi_tab.py          # SPI
│   └── gpio_tab.py         # GPIO
└── widgets/                # 通用控件
```

## 核心优势

1. **多实例支持** - 可同时打开多个同类型连接
2. **紧凑侧边栏** - 36px 图标栏，最大化内容区域
3. **可扩展性** - 添加新功能只需增加图标和标签类型
4. **分割布局** - 支持多窗口对比查看
5. **统一交互** - 所有功能遵循相同的标签页模式

## 参考资源

- Electerm 源码: `temp/electerm/`
- 侧边栏实现: `src/client/components/sidebar/`
- 标签页实现: `src/client/components/tabs/`
- 布局系统: `src/client/components/layout/`
